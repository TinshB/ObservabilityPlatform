package com.observability.apm.dto;

/**
 * Story 13.2 — Data source types supported by widget queries.
 */
public enum DataSourceType {
    PROMETHEUS,
    ELASTICSEARCH,
    JAEGER,
    POSTGRESQL
}
